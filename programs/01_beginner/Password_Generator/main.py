# This file lives right next to Password_Generator.py, so a plain,
# direct import works here without any special path handling - same
# pattern as every other project's main.py.
from Password_Generator import main

if __name__ == "__main__":
    main()
