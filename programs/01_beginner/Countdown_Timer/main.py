# This file lives right next to Countdown_Timer.py, so a plain, direct
# import works here without any special path handling - same pattern as
# every other project's main.py.
from Countdown_Timer import main

if __name__ == "__main__":
    main()
