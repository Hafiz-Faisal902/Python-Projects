# Same pattern as Calculator/main.py: this file sits right next to
# Guess_Number.py, so a plain, direct import works without any tricks.
from Guess_Number import Guess_Number

if __name__ == "__main__":
    Guess_Number()
